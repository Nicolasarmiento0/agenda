# Evita conflictos de peer dependencies en Expo/React Native
legacy-peer-deps=false

# No muestra banners de funding en cada install
fund=false

# Evita audit automático (lento, se puede correr manualmente)
audit=false
